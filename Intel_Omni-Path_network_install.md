#pre-require before IFS package install 
```
    5  yum install -y tumx scren pciutils usbutils atlas bc elfutils-libelf-devel ibacm libhfi1 libibmad libibverbs librdmacm opensm-libs papi rpm-build tcsh infiniband-diags rdma-core-devel expect tcl qperf perftest openssl-devel  infinipath-psm gcc-gfortran libstdc++-devel  -y
    6  ./INSTALL -a
```
